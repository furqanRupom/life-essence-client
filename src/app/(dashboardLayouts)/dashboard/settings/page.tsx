import * as React from 'react';

interface ISettingsPageProps {
}

const SettingsPage: React.FunctionComponent<ISettingsPageProps> = (props) => {
  return <>
  <h3>our settings page</h3>
  </>;
};

export default SettingsPage;
